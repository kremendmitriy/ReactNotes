import { Button } from '@/components/ui/button';

const AddNote = () => {
  return (
    <div>
      <Button className="w-[220px] h-[70px] rounded-none">Add Note</Button>
    </div>
  );
};

export default AddNote;
